
import 'package:flutter/material.dart';



class SupporterPage extends StatelessWidget {
  const SupporterPage({ Key? key,  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Colors.black,title:Image(image: AssetImage('images/Altrue Logo White.png'),)),
      
    );
  }
}